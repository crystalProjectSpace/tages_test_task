export enum MATERIALS {
  ALL = 0,
  WOOD = 1,
  METAL = 2,
}
  
export enum ACTION_TYPES {
  SELECT = 'selectItem',
  CHECK = 'checkItem',
}

export enum SORT_DIR {
  ASC = 'asc',
  DESC = 'desc',
}
