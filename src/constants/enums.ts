export enum MATERIALS {
  WOOD = 1,
  METAL = 2,
  ALL = 3,
}
  
export enum ACTION_TYPES {
  SELECT = 'selectItem',
  CHECK = 'checkItem',
}

export enum SORT_DIR {
  ASC = 'asc',
  DESC = 'desc',
}
